# Talaria Dashboard OS external tree.
#
# Package .mk files can be included here later if the appliance needs custom
# Buildroot packages. The first proof of concept uses only board files and a
# rootfs overlay.
